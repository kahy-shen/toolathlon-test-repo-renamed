name: Artifact
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test output" > result.txt
      - uses: actions/upload-artifact@v4
        with:
          name: test-artifact
          path: result.txt
